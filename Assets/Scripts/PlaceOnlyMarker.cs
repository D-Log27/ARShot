using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaceOnlyMarker : MonoBehaviour
{
    // 목표 : 마커 위에만 오브젝트가 위치하게 하고 싶다. 만약 카메라가 마커를 벗어나면 오브젝트가 안보이게 하고 싶다.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
