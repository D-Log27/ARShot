using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 처음 시작할 때 부터 
public class ActiveInGame_KSY : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
